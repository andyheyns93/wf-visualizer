# Loaded by `npm run dev:sample` (vite --mode sample). Shows the bundled sample workflows in
# the dev harness sidebar. Plain `npm run dev` does not load this file, so it starts empty.
VITE_SAMPLES=true
